SSH 免密登录配置完整教程
接下来要认真看
这是因为你现在用的是 “账号 + 密码” 登录方式，每次连接都得输一遍。想要免密登录，一般做法是：用 SSH 公钥认证（密钥对） 登录
下面我一步一步帮你配好 
一、核心思路先说清楚
本地电脑（Windows / Linux / WSL）：生成一对密钥
私钥：id_rsa（自己留着）
公钥：id_rsa.pub（给服务器）
远程 Linux（比如你那个 chiway 服务器）：把公钥放到 ~/.ssh/authorized_keys
设置好权限后，以后 ssh 用户名@服务器 就不用再输密码了（最多输一次私钥的 “密码短语”）。
二、本地生成 SSH 密钥对
在你用来连接服务器的那台机器上操作（Windows PowerShell / CMD / WSL / 本地 Linux 都行）。
1. 检查是否已有密钥
ls ~/.ssh
如果你能看到类似：
text
id_rsa  id_rsa.pub
说明你已经有密钥了，可以跳到第三部分「把公钥传给远程机器」。
如果没有，继续下面。
2. 生成新的密钥对
ssh-keygen -t rsa -b 4096 -C "your_email@example.com"
过程中会问你：
Enter file in which to save the key
直接回车（默认 ~/.ssh/id_rsa）
Enter passphrase（密码短语）
留空：以后连接完全不需要输入任何东西
填一个：以后连接时要输入这个密钥密码（安全更高）
生成后再看一眼：
ls ~/.ssh
# 一般会有：id_rsa  id_rsa.pub
三、把公钥放到远程服务器（最关键）
有两种常见方式，你按情况选一个。
方式 1：用 ssh-copy-id（最省心）
在本地执行（把命令中的用户名和 IP / 主机名改成你的）：
ssh-copy-id chiway@your_server_ip
它会让你再输入一次服务器密码，然后自动把公钥写进远程的 ~/.ssh/authorized_keys。
如果你执行时提示 ssh-copy-id: command not found，就用方式 2。
方式 2：手工拷贝公钥
1) 在本地查看公钥内容
cat ~/.ssh/id_rsa.pub
会输出一长串类似这样：
text
ssh-rsa AAAAB3NzaC1yc2EAAAADAQABAAACAQ... your_email@example.com
把这整行复制下来（不要少字符）。
2) 登录到远程服务器
ssh chiway@your_server_ip
# 这一步还需要密码，正常
3) 在远程服务器上操作
mkdir -p ~/.ssh
chmod 700 ~/.ssh

nano ~/.ssh/authorized_keys  # 或 vim、vi 都行
把刚才复制的那一整行公钥粘进去，保存退出。
然后：
chmod 600 ~/.ssh/authorized_keys
这一步权限一定要设对，不然 sshd 会直接忽略这个文件。
四、测试免密登录
回到本地：
ssh chiway@your_server_ip
如果配置成功，此时应该：
不再问你「用户密码」
如果你生成密钥时设置了 passphrase，只会让你输一次密钥密码
五、让 VSCode Remote-SSH 也用这个密钥
如果你是用 VSCode 远程开发（看你之前聊天记录应该是），确保 Remote-SSH 配置指定了你的密钥。
VSCode 命令面板（Ctrl+Shift+P）→ 输入并选择：
Remote-SSH: Open SSH Configuration File
选择你的 ssh 配置（一般是 ~/.ssh/config），然后写成类似这样：
text
Host my-ros2-server
    HostName your_server_ip
    User chiway
    IdentityFile ~/.ssh/id_rsa
以后只要在 VSCode 里选择 my-ros2-server 连接就行了，不用再输用户密码。
六、如果还是每次要密码，可以从这里排查
1. 检查远程 sshd 是否开启公钥登录
在远程服务器上（需要 sudo 权限）：
sudo nano /etc/ssh/sshd_config
确保下面这几行是开启的（没有被注释，值正确）：
text
PubkeyAuthentication yes
AuthorizedKeysFile      .ssh/authorized_keys .ssh/authorized_keys2
PasswordAuthentication yes  # 如果你还想保留密码登录可以设 yes，否则 no
修改后重启 sshd 服务：
sudo systemctl restart sshd
2. 检查文件权限
远程服务器上执行：
ls -ld ~
ls -ld ~/.ssh
ls -l ~/.ssh/authorized_keys
比较稳妥的权限是：
~：drwx------ 或 drwxr-xr-x
~/.ssh：drwx------
authorized_keys：-rw-------
3. 用 ssh -v 看详细原因
在本地执行：
ssh -v chiway@your_server_ip
仔细看输出里有没有：
text
Offering public key: ...
Authentication succeeded (publickey).
如果看到 Offering public key 但后面是 Server refused our key，说明远端没认你的 key，多半是：
公钥没贴完整
贴错用户目录
权限不对
sshd 配置不允许
七、顺手再提一个小优化（不想输密钥密码）
如果你在生成密钥时设置了密码短语，又不想每次都输一遍，可以用 ssh-agent。
在 Linux / WSL：
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_rsa
输一次密钥密码后，本次会话中再 ssh 就不用重复输入了
